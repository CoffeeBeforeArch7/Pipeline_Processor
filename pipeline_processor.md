# Pipeline

---

## 流水线的简单性能分析

假设5个阶段的延迟都是1ns（暂时不考虑真实LSU延迟）

|        | 阶段寄存器 | 关键路径 |  频率   | 指令执行延迟 | IPC  |
| :----: | :--------: | :------: | :-----: | :----------: | :--: |
| 单周期 |     无     |   5ns    | 200MHz  |     5ns      |  1   |
| 多周期 |     有     |   1ns    | 1000MHz |     5ns      | 0.2  |
| 流水线 |     有     |   1ns    | 1000MHz |     5ns      |  1   |

虽然指令执行延迟仍然是5ns，但流水线的频率高，IPC高

- 频率高的原因：有寄存器，关键路径短
  - **没有寄存器**的路径会累积所有嘴和逻辑延迟，而引入寄存器可以截断路径，从而减少单段延迟。
- IPC高的原因：每个周期都在处理5条不同的指令，throughput高

